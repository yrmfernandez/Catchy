# Generated from pyproject.toml [project.dependencies].
# Present alongside pyproject.toml because some platforms' Python builders
# (notably Vercel's @vercel/python) look for requirements.txt specifically and
# do not reliably resolve pyproject.toml. Keep these two files in sync — CI's
# `pip install ".[dev]"` reads pyproject.toml, this file is for those platforms.
fastapi>=0.111
python-multipart>=0.0.9
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
celery>=5.4
redis>=5.0
httpx>=0.27
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
pyjwt>=2.8
bcrypt>=4.1
email-validator>=2.1
scikit-learn>=1.5
scipy>=1.13
numpy>=1.26
joblib>=1.4
lightgbm>=4.3
