import { Placeholder } from "@/components/Placeholder";

export default function SettingsPage() {
  return (
    <Placeholder
      title="Settings"
      description="Account and detection preferences (scoring thresholds, threat-intel providers, LLM provider). These are currently configured server-side via environment variables."
      milestone="M9"
    />
  );
}
