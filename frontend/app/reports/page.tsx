import { Placeholder } from "@/components/Placeholder";

export default function ReportsPage() {
  return (
    <Placeholder
      title="Reports"
      description="Exportable PDF/CSV scan reports. The data model supports it (every scan is persisted in full), but generation isn't built yet."
      milestone="M9"
    />
  );
}
